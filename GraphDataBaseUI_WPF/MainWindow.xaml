<Window x:Class="GraphDataBaseUI_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="468" Width="775" xmlns:my="http://schemas.microsoft.com/winfx/2006/xaml/presentation/ribbon" WindowStyle="ThreeDBorderWindow" xmlns:my1="clr-namespace:System;assembly=mscorlib">
    <Window.Resources>
        <SolidColorBrush x:Key="PreussenBlue" Color="{Binding RelativeSource={RelativeSource Self}, Path=Color}"></SolidColorBrush>
    </Window.Resources>
    <DockPanel Name="dockPanel1" LastChildFill="True" DataContext="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window, AncestorLevel=1}}">
        <ListBox Name="groupBox1" Width="258">
            <StackPanel Width="250">
                <Button Content="Button" Height="30" HorizontalAlignment="Left" Name="button1" VerticalAlignment="Top" Width="112" DataContext="{Binding}">
                    <Button.LayoutTransform>
                        <RotateTransform Angle="30"></RotateTransform>
                    </Button.LayoutTransform>
                </Button>
                <Button Content="Button" Height="30" Name="button3" Width="67" RenderTransformOrigin="0.618,0.618">
                    <Button.RenderTransform>
                        <RotateTransform Angle="330"></RotateTransform>
                    </Button.RenderTransform>
                </Button>
                <Button Content="Button" Height="30" Name="button2" HorizontalAlignment="Right" Width="95" VerticalAlignment="Bottom">
                    <Button.RenderTransform>
                        <SkewTransform AngleX="30" AngleY="30" CenterX="10" CenterY="10"></SkewTransform>
                    </Button.RenderTransform>
                </Button>
                <Button Content="Button" Height="30" Name="button4" Width="137" >
                    <Button.RenderTransform>
                        <TransformGroup>
                            <RotateTransform Angle="100"></RotateTransform>
                            <SkewTransform AngleX="30" AngleY="30" CenterX="10" CenterY="10"></SkewTransform>
                        </TransformGroup>
                    </Button.RenderTransform>
                </Button>
                <Slider Height="31" Name="slider1" Maximum="100" Minimum="12" Value="12" Background="{Binding ElementName=button1, Path=Background}" />
                <TextBox Text="{Binding ElementName=slider1, Path=Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                <TextBox Text="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window, AncestorLevel=1}, Path=ActualWidth, Mode=OneWay}"></TextBox>
                <TextBox Text="{Binding Path=ActualHeight, Mode=OneWay}"></TextBox>
                <TextBox Text="{Binding Path=ActualWidth, Mode=OneWay}" ></TextBox>
                <TextBox Background="{StaticResource PreussenBlue}"></TextBox>
                <Slider Height="31" Name="slider2" Maximum="255" Minimum="0" Value="0" Background="{Binding ElementName=button1, Path=Background}" Panel.ZIndex="{Binding}" />
                <CheckBox Margin="5" IsChecked="True">Panel1</CheckBox>
            </StackPanel>
        </ListBox>
        <Grid Background="LightBlue" DockPanel.Dock="Right" Width="274" Name="Grid1">
            <!-- Define four rows: -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <!-- Define two columns: -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="100" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="4*" MinWidth="100"/>
            </Grid.ColumnDefinitions>

            <!-- Arrange the children: -->
            <Label    Grid.Row="0" Grid.Column="0" Background="Blue" Foreground="White"
            HorizontalContentAlignment="Center" Grid.ColumnSpan="3" Panel.ZIndex="1">Start Page</Label>
            
            <GroupBox Grid.Row="1" Grid.Column="0" Background="White"
            Header="Recent Projects">...</GroupBox>
            <GroupBox Grid.Row="2" Grid.Column="0" Background="White"
            Header="Getting Started">...</GroupBox>
            <GroupBox Grid.Row="3" Grid.Column="0" Background="White"
            Header="Headlines">...</GroupBox>
            <GridSplitter Grid.Column="1" Width="3" Grid.RowSpan="4" HorizontalAlignment="Center" />
            <GroupBox Grid.Row="1" Grid.Column="2" Background="White"
            Header="Online Articles" Grid.RowSpan="3">
                <ListBox DataContext="{Binding}" ItemsSource="{Binding}">
                    <ListBoxItem>Article #1</ListBoxItem>
                    <ListBoxItem>Article #2</ListBoxItem>
                    <ListBoxItem>Article #3</ListBoxItem>
                    <ListBoxItem>Article #4</ListBoxItem>
                </ListBox>
            </GroupBox>
        </Grid>
            <StackPanel >
            <StackPanel.Resources>
                <Style x:Key="ButtonStyle" TargetType="{x:Type Button}">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Button.Foreground" Value="Gold"/>
                        </Trigger>
                    </Style.Triggers>
                    <Setter Property="Button.FontSize" Value="22"/>
                    <Setter Property="Button.Background" Value="Purple"/>
                    <Setter Property="Button.Foreground" Value="White"/>
                    <Setter Property="Button.Height" Value="50"/>
                    <Setter Property="Button.Width" Value="50"/>
                    <Setter Property="Button.RenderTransformOrigin" Value="0,1"/>
                    <Setter Property="Button.RenderTransform">
                        <Setter.Value>
                            <RotateTransform Angle="10"/>
                        </Setter.Value>
                    </Setter>
                </Style>
                <Style x:Key="GoldButtonStyle" BasedOn="{StaticResource ButtonStyle}">
                    <Setter Property="Button.Background" Value="Gold"/>
                </Style>
                <ControlTemplate x:Key="buttonTemplate">
                    <Grid>
                        <Ellipse x:Name="OutCircle" >
                            <Ellipse.Fill>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Offset="0"  Color="{Binding RelativeSource={RelativeSource TemplatedParent},
                Path=Background.Color}"/>
                                    <GradientStop Offset="1" Color="Red"/>
                                </LinearGradientBrush>
                            </Ellipse.Fill>
                        </Ellipse>
                        <Ellipse RenderTransformOrigin=".5,.5">
                            <Ellipse.RenderTransform>
                                <ScaleTransform ScaleX=".8" ScaleY=".8"/>
                            </Ellipse.RenderTransform>
                            <Ellipse.Fill>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Offset="0" Color="White"/>
                                    <GradientStop Offset="1" Color="Transparent"/>
                                </LinearGradientBrush>
                            </Ellipse.Fill>
                        </Ellipse>
                        <Viewbox>
                            <ContentPresenter Margin="{TemplateBinding Button.Padding}" Content="{TemplateBinding Button.Content}"/>
                        </Viewbox>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Button.IsMouseOver" Value="True">
                            <Setter TargetName="OutCircle" Property="Fill" Value="Orange"/>
                        </Trigger>
                        <Trigger Property="Button.IsPressed" Value="True">
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="0.9" ScaleY="0.9"/>   
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Button.RenderTransformOrigin" Value="0.5,0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </StackPanel.Resources>
            <Button Template="{StaticResource buttonTemplate}"
    Height="100" Width="100" FontSize="80" Background="Black"
    Padding="20" Margin="5">1</Button>
            <Button Template="{StaticResource buttonTemplate}"
    Height="150" Width="250" FontSize="80" Background="Yellow"
    Padding="20" Margin="5">2</Button>
            <Button Template="{StaticResource buttonTemplate}"
    Height="200" Width="200" FontSize="80" Background="White"
    Padding="20" Margin="5">3</Button>
        </StackPanel>
    </DockPanel>
</Window>
